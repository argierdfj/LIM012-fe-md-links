export const mdlinks = (path, opts = {validate: false}) => {
  return [[]];
}

export const relativeToAbsolute = (path) => {
  return '';
}

export const validateRoute = (route) => {
  return true;
}

export const validateFolder = (route) => {
  return true;
}

export const validateFile = (route) => {
  return false;
}